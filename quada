<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Nghĩa Gay</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      height: 100vh;
      width: 100vw;
      background: linear-gradient(135deg, #ffafbd, #ffc3a0);
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
      font-family: 'Comic Sans MS', cursive, sans-serif;
      position: relative;
    }

    /* Trái tim trang trí */
    .heart {
      position: absolute;
      color: #ff4d94;
      font-size: 24px;
      opacity: 0.7;
      animation: float 10s infinite ease-in-out;
    }

    /* Dòng chữ chính */
    .text {
      font-size: clamp(2.5rem, 12vw, 9rem);
      font-weight: bold;
      color: white;
      text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
      z-index: 10;
      text-align: center;
      animation: pulse 2s infinite alternate;
    }

    /* Hiệu ứng tim đập */
    @keyframes pulse {
      from { transform: scale(1); }
      to { transform: scale(1.05); }
    }

    /* Hiệu ứng bay lượn cho trái tim */
    @keyframes float {
      0%, 100% {
        transform: translateY(0) rotate(0deg);
      }
      50% {
        transform: translateY(-20px) rotate(10deg);
      }
    }

    /* Tạo nhiều trái tim ngẫu nhiên */
    .heart:nth-child(1) { top: 10%; left: 10%; animation-delay: 0s; font-size: 20px; }
    .heart:nth-child(2) { top: 20%; right: 15%; animation-delay: 1s; font-size: 28px; }
    .heart:nth-child(3) { bottom: 15%; left: 20%; animation-delay: 2s; font-size: 22px; }
    .heart:nth-child(4) { bottom: 25%; right: 10%; animation-delay: 3s; font-size: 30px; }
    .heart:nth-child(5) { top: 40%; left: 5%; animation-delay: 4s; font-size: 26px; }
    .heart:nth-child(6) { top: 10%; right: 25%; animation-delay: 5s; font-size: 24px; }
    .heart:nth-child(7) { bottom: 10%; left: 50%; animation-delay: 6s; font-size: 28px; }
    .heart:nth-child(8) { top: 30%; right: 40%; animation-delay: 7s; font-size: 22px; }
  </style>
</head>
<body>
  <!-- Trái tim dùng ký tự ❤ -->
  <div class="heart">❤</div>
  <div class="heart">❤</div>
  <div class="heart">❤</div>
  <div class="heart">❤</div>
  <div class="heart">❤</div>
  <div class="heart">❤</div>
  <div class="heart">❤</div>
  <div class="heart">❤</div>

  <div class="text">Nghĩa Gay</div>
</body>
</html>
